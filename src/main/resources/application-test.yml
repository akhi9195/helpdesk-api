# Automated tests (No datasource here: Testcontainers supplies it)
Spring:
  jpa:
    properties:
      hibernate:
        generate_statistics: true

logging:
  level:
    org.hibernate.SQL: debug
    root: WARN
    com.portfolio.helpdesk: INFO

app:
  jwt:
    secret: <generate a second value just for tests>